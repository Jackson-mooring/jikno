import { Routes } from '@angular/router';

import { AccountComponent } from '../components/account.component';

export const AccountRoutes: Routes = [
	{ path: 'user-settings', component: AccountComponent },
]